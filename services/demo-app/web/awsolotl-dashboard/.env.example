# Base URL of the control-api this dashboard talks to.
# Local default (see docs/RUNNING_LOCALLY.md): http://localhost:8090
# Once deployed against real AWS, point this at the deployed control-api/API Gateway URL.
VITE_CONTROL_API_URL=http://localhost:8090
